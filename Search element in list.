#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
int N, search_value, found = 0;
    scanf("%d", &N);
    int arr[N];
    
    for (int i = 0; i < N; i++) {
        scanf("%d", &arr[i]);
    }

    scanf("%d", &search_value);

    for (int i = 0; i < N; i++) {
        if (arr[i] == search_value) {
            found = 1;
            break;
        }
    }

    if (found) {
        printf("Found\n");
    } else {
        printf("Not Found\n");
    }



   


    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
